// Conditional statements

{
    let age = 16;

    if(age >= 18) {
        console.log("You can vote.");
    }

    if(age < 18) {
        console.log("You con not vote")
    }
};
{
    let mode = "dark";
    let color;

    if(mode == "dark") {
        color = "Black";
    } else {
        color = "White";
    }

    console.log(color);
 };
